import { createTheme } from "@mui/material"

const theme = createTheme({
    palette: {
        primary: {
        main: '#1b8af2',
        },
        secondary: {
        main: '#c2e9f2',
        },
    },
})

  export default theme
